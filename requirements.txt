boto3>=1.34
pymysql>=1.1
python-dotenv>=1.0
pandas>=2.1

# Optional prompt-log backends — install only the one(s) you use:
#   pip install -r requirements-postgres.txt      # --backend postgres
#   pip install -r requirements-opensearch.txt    # --backend opensearch | elasticsearch
#   pip install -r requirements-clickhouse.txt    # --backend clickhouse
# MySQL is the default backend and needs nothing beyond pymysql above.
